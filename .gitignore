node_modules/
dist/
out/
*.log
.DS_Store
tmp/
dist-tools/
/*.png
lab_format.pdf
release/
build/icon.iconset/

# tap は隣のディレクトリに置く。ここにクローンしても埋め込みリポジトリとして
# コミットされないようにするための保険
homebrew-jtalk-gui/

# npm run cask で毎回生成されるため追跡しない（正はtapリポジトリ側）
homebrew/Casks/*.rb
